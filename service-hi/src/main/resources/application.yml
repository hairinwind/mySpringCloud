server:
  port: ${SERVER_PORT}
# ${SERVER_PORT} 8762

spring:
  application:
    name: service-hi
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/